[package]
name = "transcribeit"
version = "2.0.0"
edition = "2024"
rust-version = "1.96"
license-file = "LICENSE"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = "symbols"
incremental = false

[build-dependencies]
protoc-bin-vendored = "3.2.0"
tonic-prost-build = "0.14.6"

[dependencies]
anyhow = "1"
async-trait = "0.1"
clap = { version = "4", features = ["derive", "env"] }
dotenvy = "0.15"
futures-util = "0.3"
hound = "3.5"
glob = "0.3"
indicatif = "0.18"
reqwest = { version = "0.13", features = ["json", "multipart", "stream"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.11"
tempfile = "3"
regex = "1"
urlencoding = "2"
tokio = { version = "1", features = ["full"] }
whisper-rs = "0.16"
bytes = "1.11.1"
aws-config = { version = "1.8.18", default-features = false, features = ["default-https-client", "rt-tokio"] }
# Keep the modern HTTPS client without the legacy Hyper 0.14 / Rustls 0.21 transport.
aws-sdk-s3 = { version = "1.135.0", default-features = false, features = ["sigv4a", "http-1x", "default-https-client", "rt-tokio"] }
aws-credential-types = "1.2.14"
uuid = { version = "1.23.3", features = ["v4"] }
tonic = { version = "0.14.6", features = ["tls-webpki-roots"] }
prost = "0.14.4"
tonic-prost = "0.14.6"
